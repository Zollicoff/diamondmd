/**
 * App-wide named constants. Anything we want to swap centrally instead
 * of grepping for a magic number lives here.
 */

/** Words-per-minute used by NoteView's reading-time estimate. */
export const READING_SPEED_WPM = 220;
